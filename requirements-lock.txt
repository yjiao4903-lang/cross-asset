 # Fallback dependency lock generated because uv is unavailable on this host.
 # Source of truth remains pyproject.toml; versions are intentionally not invented.
 numpy>=2.0
 pandas>=2.2
 pydantic>=2.7
 pydantic-settings>=2.2
 pyyaml>=6.0
 typer>=0.12
 rich>=13.7
 duckdb>=1.0
 pytest>=8.0
 ruff>=0.6
 mypy>=1.10
