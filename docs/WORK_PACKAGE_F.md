# 工作包 F：本周读法

状态 `DEVELOPMENT_PRIOR`。解决的是周报像填空模板，不是再加一套区制标签。

## 为什么这一刀

卖方周报先写本周发生了什么、哪条腿被价格确认、下一个窗口是什么。
四格 / 胜率赔率留在身后当证据。
不加 Risk-On 仓、不加优化器、不爬实时新闻。

## 交付

- `weekly_digest.py` 用本周数字写四句话：走势、主导矛盾、纪要分歧、下周六窗口。
- `config/weekly_calendar.yml` 是人工日历，不是 Live feed。
- 默认仍不行动。
